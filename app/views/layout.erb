<!DOCTYPE html>
<html lang="en">
<head>
  <!--
    normalize.css removes cross-browser differences in defaults, e.g.,
    differences in how form elements appear between Firefox and IE
    See: http://necolas.github.com/normalize.css/
  -->
  <link rel="stylesheet" href="/css/normalize.css">
  <!--
    application.css is where you put your styles
  -->
  <link rel="stylesheet" href="/css/application.css">

  <script src="//ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js"></script>
  <script src="/js/application.js"></script>

  <title>End User Strikes Back!</title>
    
</head>

<body>
<nav>
    
    <!-- Should be an if here for logged_in? (Imagine I wrote that) -->
    <a href="/">Home</a> |
    <% if logged_in? %>
      <a href="/users/<%=current_user.id%>" id='current_user'><%= current_user.username %></a> |
      <a href='/sessions/<%= current_user.id %>' id='logout'>Logout</a>
    <% else %>
     <a href="/users/new" id='register'>Register</a>
    <% end %> |
      <a href='' id='login'>Login</a>
  </nav>
  <div id='form-catcher'></div>
  <div class='container'>
<h3 class='nav-header'>End Users Strike Back</h3>
  
    <%= yield %>
</body>
</html>
